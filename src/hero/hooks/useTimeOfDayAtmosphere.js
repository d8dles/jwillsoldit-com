import { palette } from '../config/heroTheme'

export function useTimeOfDayAtmosphere() {
  // Phase 1 stub — returns static palette.
  // Phase 2 will add dynamic time-of-day shift.
  return palette
}
