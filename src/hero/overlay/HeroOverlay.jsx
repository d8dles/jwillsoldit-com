import styles from '../../styles/hero.module.css'

export default function HeroOverlay() {
  return (
    <div className={styles.overlayLayer}>
      {/* wordmark, button, and transition content mount here in later tasks */}
    </div>
  )
}
